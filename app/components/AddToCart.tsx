"use client";

const AddToCart = () => {
  return (
    <div
      className="btn btn-primary"
      onClick={() => {
        console.log("click");
      }}
    >
      AddToCart
    </div>
  );
};

export default AddToCart;
